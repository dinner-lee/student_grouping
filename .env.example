# Postgres 연결 문자열 (로컬 / Neon / Supabase / Vercel Postgres 등)
DATABASE_URL=postgresql://user:password@localhost:5432/student_grouping

# `openssl rand -base64 32` 로 생성
AUTH_SECRET=
AUTH_TRUST_HOST=true

# Google 로그인 활성화 시 (선택):
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=
